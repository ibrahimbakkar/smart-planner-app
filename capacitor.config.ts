import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ibrahim.smartplanner',
  appName: 'المنظم الزمني الذكي',
  webDir: 'www'
};

export default config;
